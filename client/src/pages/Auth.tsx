import React from 'react';
import AuthForm from '../components/AuthForm';

const Auth: React.FC = () => {
  return (
    <div className="Auth">
      <AuthForm />
    </div>
  );
};

export default Auth;
